/* NOTE: Post logic (create, delete, like, comment, render) is handled
   entirely by storage.js via the Posts object and UI.renderPostCard().
   
   This file is intentionally empty and kept for project structure clarity.
   Do NOT add duplicate function definitions here — they would silently
   shadow the ones in storage.js since all scripts share global scope. */